Fix/consolidate readline methods
Find out how with cmake to have one jr_assert library in top level dir or "install" it and use find_package as each subdirectory having it's own copy is dumb and they are now different
Optimisation for nodes similar to as before, as always go if contains(node) then contains_prefix(node, "abcdef") but we start the contains_prefix search from the start again
